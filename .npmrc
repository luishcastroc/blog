resolution-mode=highest
# pnpm 11 runs a deps-status check before every `pnpm run` script. It exits
# non-zero on unapproved build scripts (esbuild, nx, lmdb, …), which blocked
# `pnpm start`. Builds are approved in pnpm-workspace.yaml; skip the pre-run check.
verify-deps-before-run=false
