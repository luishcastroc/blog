<script lang="ts">
  import { ReactiveFormsModule } from '@angular/forms';
  import ContactForm from '../components/contact-form.analog' with { analog: 'imports' };
  import { RouteMeta } from '@analogjs/router';

  export const routeMeta: RouteMeta = {
    title: 'Luis Castro - Contact',
  };
  defineMetadata({ imports: [ReactiveFormsModule] });
</script>

<template>
  <ContactForm />
</template>

