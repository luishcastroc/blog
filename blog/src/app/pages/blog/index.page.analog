<script lang="ts">
  import { RouteMeta } from '@analogjs/router';
  import BlogList from '../../components/blog-list.analog' with { analog: 'imports' };

  export const routeMeta: RouteMeta = {
    title: 'Luis Castro - Blog',
    meta: [{ name: 'description', content: 'Blog Posts' }],
  };
</script>

<template>
  <BlogList />
</template>
